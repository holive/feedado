variables:
  GIT_STRATEGY: 'fetch'
  GET_SOURCES_ATTEMPTS: '3'
  PROJECT_PATH: '/go/src/gitlab.vpc-xpto-01/oss/gopkg.git'

before_script:
  - set -e
  - mkdir -p $PROJECT_PATH
  - cp -r $CI_PROJECT_DIR/* $PROJECT_PATH
  - cd $PROJECT_PATH

test:
  image: golang:1.12.6
  script: make test